
class User {
    postsighup(email, password) {
        console.log(`successfully inject to controller email ${email}`)
        console.log(`successfully inject to controller email ${password}`)
        return
    }
}

module.exports = User